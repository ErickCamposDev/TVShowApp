import React, { FC } from 'react';
import { Div, Text } from 'react-native-magnus';

export const Favorites: FC = ({}) => {
  return (
    <Div flex={1}>
      <Text>Favorites</Text>
    </Div>
  );
};
